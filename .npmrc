# Required because @typescript-eslint v8 declares peer dep typescript <6.0.0,
# but actually works fine with TypeScript 6. Remove once @typescript-eslint v9
# is released with updated peer deps.
legacy-peer-deps=true
